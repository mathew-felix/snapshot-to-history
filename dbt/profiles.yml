scd2_registry:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'scd2user') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'scd2pass') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'scd2db') }}"
      schema: public
      threads: 4
